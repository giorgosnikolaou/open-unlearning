# @package eval.rwku.metrics.neighbor_level1_ROUGE
defaults:
  - ../../data/datasets@datasets: RWKU_neighbor_level1
  - ../../collator@collators: DataCollatorForSupervisedDatasetwithIndex
  - ../../generation@generation_args: default

handler: rouge
rouge_type: rougeL_recall
batch_size: ${eval.rwku.batch_size}

datasets:
  RWKU_neighbor_level1:
    args:
      hf_args:
        filter_column: "subject"
        filter_values: ${eval.rwku.forget_subjects}
      predict_with_generate: True
      system_prompt_suffix: "Please complete the blank in the following question."

collators:
  DataCollatorForSupervisedDataset:
    args:
      padding_side: left

generation_args:
  max_new_tokens: 100
  stopwords: ["\n\n"]
